<?php 
/**
 * This is your email config file, you can have multiple configurations which
 * which you can switch between.
 *
 */
use Origin\Utility\Email;

Email::config('default',[
        'host' => 'smtp.gmail.com',
        'port' => 465,
        'username' => 'email@gmail.com',
        'password' => 'secret',
        'timeout' => 30,
        'ssl' => true,
        'tls' => false
        ]
    );
